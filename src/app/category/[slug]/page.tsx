import{categories}from"@/lib/products";import{ShopClient}from"@/components/shop-client";import{notFound}from"next/navigation";export function generateStaticParams(){return categories.map(c=>({slug:c.slug}))}export default async function Category({params}:{params:Promise<{slug:string}>}){const{slug}=await params,c=categories.find(x=>x.slug===slug);if(!c)notFound();return <><div className="page-title"><div className="container"><span className="eyebrow">CATEGORY</span><h1>{c.name}</h1></div></div><section className="section container"><ShopClient category={slug}/></section></>}
