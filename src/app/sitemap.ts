import type{MetadataRoute}from"next";import{categories,products}from"@/lib/products";export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000",pages=["","shop","about","contact","faq","shipping-policy","returns-policy","privacy-policy","terms"];return[...pages.map(x=>({url:`${base}/${x}`,lastModified:new Date()})),...products.map(p=>({url:`${base}/products/${p.slug}`,lastModified:new Date()})),...categories.map(c=>({url:`${base}/category/${c.slug}`,lastModified:new Date()}))]}
