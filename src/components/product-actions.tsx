"use client";import{useState}from"react";import{useRouter}from"next/navigation";import type{Product}from"@/lib/products";import{useCart}from"./cart-provider";export function ProductActions({p}:{p:Product}){const[q,setQ]=useState(1),{add}=useCart(),r=useRouter();return <><div className="qty"><button onClick={()=>setQ(Math.max(1,q-1))}>−</button><span>{q}</span><button onClick={()=>setQ(Math.min(p.stock,q+1))}>+</button></div><div className="actions"><button className="btn primary" onClick={()=>add(p,q)}>Add to Cart</button><button className="btn secondary" onClick={()=>{add(p,q);r.push("/checkout")}}>Buy Now</button></div></>}
