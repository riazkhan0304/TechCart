"use client";import{useMemo,useState}from"react";import{categories,products}from"@/lib/products";import{ProductCard}from"./product-card";export function ShopClient({category=""}:{category?:string}){const[q,setQ]=useState(""),[cat,setCat]=useState(category),[max,setMax]=useState(10000),[stock,setStock]=useState(false),[sort,setSort]=useState("featured");const list=useMemo(()=>products.filter(p=>(!q||p.name.toLowerCase().includes(q.toLowerCase()))&&(!cat||p.categorySlug===cat)&&p.price<=max&&(!stock||p.stock>0)).sort((a,b)=>sort==="low"?a.price-b.price:sort==="high"?b.price-a.price:sort==="new"?Number(b.isNew)-Number(a.isNew):Number(b.featured)-Number(a.featured)),[q,cat,max,stock,sort]);return <div className="shop-layout"><aside className="filters"><h3>Filters</h3><label>Search</label><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search products..."/><label>Category</label><select value={cat} onChange={e=>setCat(e.target.value)}><option value="">All categories</option>{categories.map(c=><option key={c.slug} value={c.slug}>{c.name}</option>)}</select><label>Maximum price: Rs. {max.toLocaleString()}</label><input type="range" min="500" max="10000" step="500" value={max} onChange={e=>setMax(+e.target.value)}/><label><input type="checkbox" checked={stock} onChange={e=>setStock(e.target.checked)}/> In stock only</label></aside><div><div className="shop-top"><span>{list.length} products</span><select value={sort} onChange={e=>setSort(e.target.value)}><option value="featured">Featured</option><option value="new">Newest</option><option value="low">Price: Low to High</option><option value="high">Price: High to Low</option></select></div><div className="product-grid">{list.map(p=><ProductCard key={p.id} product={p}/>)}</div></div></div>}
