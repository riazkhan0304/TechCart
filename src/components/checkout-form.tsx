"use client";import{FormEvent,useState}from"react";import{money}from"@/lib/products";import{useCart}from"./cart-provider";export function CheckoutForm(){const{items,subtotal,clear}=useCart(),[order,setOrder]=useState<string>(),[loading,setLoading]=useState(false),shipping=items.length?250:0;async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const data=Object.fromEntries(new FormData(e.currentTarget));const res=await fetch("/api/orders",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({customer:data,items:items.map(i=>({id:i.product.id,quantity:i.quantity})),paymentMethod:"COD"})});const json=await res.json();if(res.ok){setOrder(json.orderNumber);clear()}setLoading(false)}if(order)return <div className="empty container"><span>✓</span><h2>Thank you for your order.</h2><p>Order number: <b>{order}</b></p><p>Payment method: Cash on Delivery · Status: Pending</p></div>;if(!items.length)return <div className="empty"><h2>Your cart is empty</h2></div>;return <form onSubmit={submit} className="checkout-layout"><div><h1>Checkout</h1><div className="form-grid">{[["fullName","Full Name","text"],["mobile","Mobile Number","tel"],["email","Email Address","email"],["province","Province","text"],["city","City","text"],["postalCode","Postal Code","text"]].map(x=><label key={x[0]}>{x[1]}<input className="field" name={x[0]} type={x[2]} required/></label>)}<label className="wide">Complete Address<textarea className="field" name="address" required/></label><label className="wide">Order Notes<textarea className="field" name="notes"/></label></div><h2>Payment method</h2><div className="payment"><b>● Cash on Delivery</b><p>Pay when your order arrives, subject to store confirmation.</p></div><div className="payment disabled"><b>○ Online Card Payment — Coming Soon</b><p>Bank Alfalah will only be enabled after official credentials and testing.</p></div></div><aside className="summary"><h2>Your Order</h2>{items.map(i=><p key={i.product.id}><span>{i.product.name} × {i.quantity}</span><b>{money(i.product.price*i.quantity)}</b></p>)}<p><span>Shipping</span><b>{money(shipping)}</b></p><p className="total"><span>Total</span><b>{money(subtotal+shipping)}</b></p><button className="btn primary" disabled={loading}>{loading?"Placing order...":"Place COD Order"}</button></aside></form>}
